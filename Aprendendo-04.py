# Operador IF do Python || AND no python é "and" e OR no python é "or" || IF no python funciona apenas por tabulação, para sair,
# basta quebrar a tabulação denominada
# Operadores lógicos: == != < <= => > 
#var_verdade = True Boolean
#var_false   = False

a = 1
b = 2

if (a <= b and 'abacaxi' == 'uva') or 1 != 2:
    print('A variável', a, 'é menor ou igual a variável', b)
else:
    print('A variável', a, 'é maior que a variável', b)